apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appset-env-generator-plugin.fullname" . }}-test-connection"
  labels:
    {{- include "appset-env-generator-plugin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appset-env-generator-plugin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
